# macOS Finder metadata. Created any time you open a folder in Finder.
.DS_Store
._*

# Local upload storage (LocalFileStorage default root).
api/var/

# Local secrets scratch file (Gemini key, Google client secret) — never commit.
keys.txt

# Redis dump produced by a local redis-server.
*.rdb
